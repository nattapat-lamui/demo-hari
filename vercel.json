{
  "framework": "vite",
  "buildCommand": "npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/((?!api|assets|favicon|static).*)", "destination": "/index.html" }
  ]
}
